# ============================================================
# requirements.txt — viral_clips pipeline
# Python 3.13 · Windows 11 · NVIDIA RTX 4080 SUPER
# ============================================================

# ── Core IA / Transcripción ──────────────────────────────────
faster-whisper

# PyTorch con CUDA 12.8 (RTX 4080 SUPER / driver CUDA 13.x)
# IMPORTANTE: No instalar con pip install torch directamente (instala CPU-only)
# Usar siempre:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
torch
torchvision
torchaudio

# ── Google Gemini (nuevo SDK — reemplaza google-generativeai) ─
google-genai

# ── Diarización de locutores (opcional) ─────────────────────
# Requiere variable de entorno HF_TOKEN y aceptar términos en:
# https://hf.co/pyannote/speaker-diarization-3.1
pyannote.audio

# ── Visión por computador ────────────────────────────────────
opencv-python
numpy

# ── Imágenes / animación neon ────────────────────────────────
Pillow

# ── Interfaz web ─────────────────────────────────────────────
streamlit

# ── Modo watcher automático ───────────────────────────────────
watchdog

# ── Publicación YouTube ──────────────────────────────────────
# Requiere client_secrets.json y token.pickle
google-api-python-client
google-auth-oauthlib

# ── Publicación TikTok ───────────────────────────────────────
# Requiere cookies.txt
tiktok-uploader
playwright
# Tras instalar playwright ejecutar: playwright install

# ============================================================
# INSTALACIÓN COMPLETA
# ============================================================
#
# 1. Dependencias base:
#      pip install -r requirements.txt
#
# 2. PyTorch con CUDA (obligatorio para GPU):
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# 3. Playwright (solo auto-publicación TikTok):
#      playwright install
#
# 4. FFmpeg con NVENC — instalar manualmente y añadir al PATH:
#      https://ffmpeg.org/download.html
#
# ============================================================
# VARIABLES DE ENTORNO REQUERIDAS (PowerShell $PROFILE)
# ============================================================
#
#   $env:GEMINI_API_KEY           = "tu_clave_aqui"
#   $env:HF_TOKEN                 = "tu_token_aqui"   # Solo si usas diarización
#   $env:PYTHONIOENCODING         = "utf-8"
#   $env:PYTHONUTF8               = "1"
#   $env:HF_HUB_DISABLE_SYMLINKS_WARNING = "1"
#
